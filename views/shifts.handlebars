<body>
    <table style=" color:blue; border:1px solid black">
        <tr style="border:1px solid black">
            {{#each data}}
            <td><th style="border:1px solid black">{{this.shift}}</th></td>

        </tr>
        <tr>
            <td>Available Waiters</td>
        </tr>
        <tr>
            <td style="border:1px solid black">{{this.waiter}}</td>
        </tr>
        {{/each}}

    </table>
</body>